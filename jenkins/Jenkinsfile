pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Verify Environment') {
            steps {
                sh 'pwd'
                sh 'ls -la'
                sh 'docker --version'
                sh 'docker ps'
            }
        }

        stage('Build Frontend Image') {
            steps {
                sh 'docker build -t infrapilot-frontend:latest -f docker/frontend.Dockerfile .'
            }
        }

        stage('Build Backend Image') {
            steps {
                sh 'docker build -t infrapilot-backend:latest -f docker/backend.Dockerfile .'
            }
        }

        stage('Build Nginx Image') {
            steps {
                sh 'docker build -t infrapilot-nginx:latest nginx'
            }
        }

        stage('Verify Images') {
            steps {
                sh 'docker images'
            }
        }
    }

    post {
        success {
            echo 'InfraPilot Docker images built successfully.'
        }

        failure {
            echo 'Pipeline failed.'
        }
    }
}